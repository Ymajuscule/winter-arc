# Copy to .env and fill in from your Supabase project's Settings > API page.
# EXPO_PUBLIC_* vars are inlined into the client bundle at build time (Expo's
# convention) — never put a service-role key or other secret in this file.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
